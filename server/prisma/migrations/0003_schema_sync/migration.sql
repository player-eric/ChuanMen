-- 0003_schema_sync
-- Captures schema changes that were applied via `prisma db push` but not tracked
-- in migrations: pinned columns, lottery fields, and other additions.
-- This migration is intentionally empty — the actual changes are already applied
-- to the production database via db push.

-- Placeholder: no SQL needed (schema already matches)
SELECT 1;
